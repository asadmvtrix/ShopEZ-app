# Copy to .env and fill in from Supabase → Project Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# After setting env vars, run supabase/setup.sql once in the Supabase SQL Editor
# (creates products, orders, RLS, seed catalogue, and delete_own_account).

# Optional later (Stage 3 — Stripe / real gateway)
# VITE_PAYMENT_API_URL=
